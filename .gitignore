node_modules/
dist/
.vite/
*.log
.DS_Store

# Full-size camera masters for the DrinksStrip videos (~232MB). The web-sized
# 720p re-encodes in src/assets/pm*.mp4 are what ship; these stay local.
src/assets/video-originals/
